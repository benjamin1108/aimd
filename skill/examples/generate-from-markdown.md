# Generate From Markdown

```bash
aimd new report.aimd --input report.md --embed-local-images
aimd doctor report.aimd --json
```

Use `--title "Title"` when the file name is not a good document title.
